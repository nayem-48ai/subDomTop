name: Extract Zip and Update Main

on:
  push:
    branches:
      - main
  workflow_dispatch: # এটি দিলে আপনি নিজে বাটন চেপেও রান করতে পারবেন

jobs:
  unzip-and-update:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Find and Extract Zip
        run: |
          # জিপ ফাইল আছে কিনা চেক করা (Error এড়াতে 2>/dev/null ব্যবহার করা হয়েছে)
          ZIP_FILE=$(ls *.zip 2>/dev/null | head -n 1)
          
          if [ -n "$ZIP_FILE" ] && [ -f "$ZIP_FILE" ]; then
            echo "Found: $ZIP_FILE. Extracting now..."
            # -o দিয়ে রিপ্লেস করা হবে
            unzip -o "$ZIP_FILE" -d .
            
            echo "Deleting $ZIP_FILE..."
            rm "$ZIP_FILE"
          else
            echo "No zip file found to extract. Skipping..."
            exit 0
          fi

      - name: Save and Push Changes
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          
          git add .
          
          if git diff --quiet && git diff --staged --quiet; then
            echo "Nothing to update."
          else
            git commit -m "chore: auto-extract and overwrite files"
            git push origin main
          fi
