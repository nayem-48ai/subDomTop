name: Final Fix - Preview and Deploy

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build-and-preview:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Extract Zip
        run: |
          ZIP_FILE=$(ls *.zip 2>/dev/null | head -n 1) || true
          if [ -f "$ZIP_FILE" ]; then
            unzip -o "$ZIP_FILE" -d .
            rm "$ZIP_FILE"
          fi

      - name: Robust Vite Config Fix
        run: |
          node -e "
          const fs = require('fs');
          const file = fs.readdirSync('.').find(f => f.startsWith('vite.config.'));
          if (file) {
            let content = fs.readFileSync(file, 'utf8');
            if (!content.includes('base:')) {
              content = content.replace(/return\s*\{/, \"return {\n      base: './',\");
            }
            const serverConfig = \"server: { host: '0.0.0.0', port: 3000, allowedHosts: true },\";
            if (content.includes('server:')) {
              content = content.replace(/server\s*:\s*\{[\s\S]*?\},/, serverConfig);
            } else {
              content = content.replace(/return\s*\{/, \"return {\n      \" + serverConfig);
            }
            fs.writeFileSync(file, content);
          }
          "

      - name: Save Changes to Main Branch
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add .
          git diff --quiet && git diff --staged --quiet || (git commit -m "fix: sync changes" && git push origin main)

      - name: Install Dependencies
        run: npm install

      - name: Live Dev Preview @Tnayem48
        run: |
          # Cloudflared ইন্সটল
          curl -L --output cloudflared.deb https://github.com/cloudflare/cloudflared/releases/latest/download/cloudflared-linux-amd64.deb
          sudo dpkg -i cloudflared.deb
          
          # Vite রান করা
          npm run dev -- --host 0.0.0.0 --port 3000 & 
          
          sleep 10
          
          # টানেল চালু করা এবং লগে লিঙ্ক সেভ করা
          timeout 10m cloudflared tunnel --url http://localhost:3000 2>&1 | tee /tmp/cf.log &
          
          # লিঙ্ক জেনারেট হওয়া পর্যন্ত অপেক্ষা
          sleep 10
          
          # লিঙ্কটি ফিল্টার করে বের করা
          CF_URL=$(grep -oE "https://[a-zA-Z0-9.-]+\.trycloudflare\.com" /tmp/cf.log | head -n 1)
          
          # GitHub Step Summary-তে বড় এবং সুন্দর লিঙ্ক পাঠানো
          echo "### 🚀 Preview is Ready!" >> $GITHUB_STEP_SUMMARY
          echo "Your dev server is live at:" >> $GITHUB_STEP_SUMMARY
          echo "## [👉 Click Here to Open Preview]($CF_URL)" >> $GITHUB_STEP_SUMMARY
          echo "---" >> $GITHUB_STEP_SUMMARY
          echo "User: @Tnayem48" >> $GITHUB_STEP_SUMMARY
          
          echo "------------------------------------------------------------"
          echo "🚀 PREVIEW LINK: $CF_URL"
          echo "------------------------------------------------------------"
          
          # ১০ মিনিট প্রিভিউ সেশন চালু রাখা
          sleep 10m || true

      - name: Final Build and Deploy
        run: |
          npm run build
          touch dist/.nojekyll

      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./dist
          publish_branch: gh-pages
          force_orphan: true
